'use client';

import { useState } from 'react';
import Link from 'next/link';
import {
  CheckCircle2,
  ArrowRight,
  Clock,
  ShieldCheck,
  Sparkles,
  SlidersHorizontal,
  DollarSign,
  Globe,
  MessageCircle,
} from 'lucide-react';
import Breadcrumb from '@/components/ui/Breadcrumb';
import FAQAccordion from '@/components/ui/FAQAccordion';
import { generateDirectWhatsAppUrl } from '@/lib/whatsapp';
import { FAQItem } from '@/types';

type Currency = 'USD' | 'SGD' | 'IDR';

interface PackageData {
  id: string;
  name: string;
  tagline: string;
  idealFor: string;
  prices: {
    USD: string;
    SGD: string;
    IDR: string;
  };
  turnaroundDays: string;
  maxPages: string;
  revisionCount: string;
  highlights: string[];
  features: string[];
  isPopular?: boolean;
}

const PACKAGES_EN: PackageData[] = [
  {
    id: 'starter',
    name: 'Starter Business Package',
    tagline: 'Rapid online presence for micro-enterprises & single-product validation',
    idealFor: 'B2B specialists, boutique consultants, single-service landing pages, and rapid market validation.',
    prices: {
      USD: '$80',
      SGD: 'S$110',
      IDR: 'Rp 1.250.000',
    },
    turnaroundDays: '3 - 5 Business Days',
    maxPages: 'Up to 3 Pages',
    revisionCount: '2 Rounds of Revisions',
    highlights: [
      'Pre-rendered Static Site Generation (SSG)',
      'Sub-second page load speeds on mobile',
      'Instant WhatsApp & Lead Form routing',
    ],
    features: [
      'Up to 3 Custom Structured Pages',
      '100% Mobile & Tablet Responsive Layout',
      'Direct WhatsApp Click-to-Chat CTA',
      'Google Maps & Location Integration',
      'SSL / TLS 1.3 HTTPS Security Setup',
      'Basic On-Page Technical SEO',
      '14-Day Post-Launch Bug Warranty',
    ],
  },
  {
    id: 'business',
    name: 'Professional Business Package',
    tagline: 'The complete digital foundation for growing B2B companies & service providers',
    idealFor: 'Mid-sized companies, manufacturers, contractors, clinics, and professional corporate profiles.',
    prices: {
      USD: '$190',
      SGD: 'S$250',
      IDR: 'Rp 2.950.000',
    },
    turnaroundDays: '7 - 10 Business Days',
    maxPages: 'Up to 7 Pages',
    revisionCount: '4 Rounds of Revisions',
    isPopular: true,
    highlights: [
      'Full Schema.org JSON-LD Structured Data',
      'Interactive Services & Portfolio Filtering',
      '60-Day Full Maintenance Warranty',
    ],
    features: [
      'Up to 7 Custom Structured Pages',
      'Modular Service/Product Catalog Grid',
      'Interactive Multi-Filter & Search',
      'Structured Request for Proposal (RFP) Form',
      'Complete Schema.org (Organization, LocalBusiness, FAQ)',
      '100/100 Core Web Vitals Optimization',
      'High-Resolution Commercial Stock Curation',
      '60-Day Technical Maintenance Warranty',
    ],
  },
  {
    id: 'enterprise',
    name: 'Enterprise Custom Package',
    tagline: 'Custom software architecture & multi-language platform for international exporters',
    idealFor: 'International exporters, large manufacturers, law firms, educational institutions, and multi-brand holdings.',
    prices: {
      USD: '$420+',
      SGD: 'S$560+',
      IDR: 'Rp 6.500.000+',
    },
    turnaroundDays: '14 - 21 Business Days',
    maxPages: 'Unlimited Pages',
    revisionCount: 'Unlimited Until Staging Approval',
    highlights: [
      'Multi-Language Framework (EN / ID / AR)',
      'Digital Brochure & Spec Sheet Downloads',
      '1-Year Priority SLA Support',
    ],
    features: [
      'Unlimited Dynamic Static Pages',
      'Multi-Language Architecture (English / Indonesian)',
      'Gated PDF Brochure & Spec Sheet Download Portal',
      'Custom Cost Estimator & Quote Calculator',
      'Bespoke Interactive Components & Animations',
      'Team Content Management Handover Training',
      'Cloudflare Edge Enterprise DDoS Shield',
      '1-Year Priority SLA Support Retainer',
    ],
  },
];

const PACKAGES_FAQS_EN: FAQItem[] = [
  {
    question: 'How do revision rounds work during development?',
    answer:
      'Each package includes dedicated structured revision milestones. Starter includes 2 comprehensive revision rounds, Business includes 4 rounds, and Enterprise provides unlimited iterative refinement until the staging preview is officially approved.',
  },
  {
    question: 'Are domain names and cloud hosting included in the package?',
    answer:
      'You may connect your own existing domain and hosting at no extra charge, or request our integrated turnkey package (including .com/.id domain registration and guaranteed fast NVMe Cloud SSD hosting).',
  },
  {
    question: 'Are there any recurring monthly licensing fees?',
    answer:
      'Zero. We charge zero proprietary CMS software license fees or monthly code retainers. The website is your permanent digital asset. Future expenses are strictly standard annual registrar renewals (domain and hosting) directly payable to your registrar.',
  },
  {
    question: 'How do international payments and invoices work?',
    answer:
      'We generate official B2B corporate invoices and tax receipts. Payments can be settled via international SWIFT bank transfer, Wise Business, or corporate card in USD, SGD, or IDR.',
  },
];

export default function EnglishWebsitePackagesPage() {
  const [currency, setCurrency] = useState<Currency>('USD');

  const getWhatsAppUrlForPackage = (pkgName: string) => {
    return generateDirectWhatsAppUrl(
      `Hello JasaWebsite team, I would like to inquire about the ${pkgName} in ${currency}.`
    );
  };

  return (
    <div className="bg-slate-50 min-h-screen">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <Breadcrumb
          items={[
            { name: 'Home', url: '/en' },
            { name: 'Website Packages', url: '/en/website-packages' },
          ]}
        />
      </div>

      {/* Page Header */}
      <section className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 sm:py-16 text-center">
        <span className="text-xs font-bold uppercase tracking-wider text-blue-600 bg-blue-50 px-3 py-1 rounded-full border border-blue-200">
          Global Pricing &amp; Scope Transparency
        </span>
        <h1 className="text-3xl sm:text-5xl font-extrabold text-slate-900 mt-4 tracking-tight">
          Website Engineering Packages
        </h1>
        <p className="text-slate-600 text-sm sm:text-base max-w-2xl mx-auto mt-3 leading-relaxed">
          Select the optimal engineering scope for your organizational scale. From single-service market validation to multi-lingual enterprise portals.
        </p>

        {/* Currency Switcher Tabs */}
        <div className="mt-8 inline-flex items-center p-1 rounded-xl bg-slate-200/80 border border-slate-300 shadow-2xs">
          {(['USD', 'SGD', 'IDR'] as Currency[]).map((c) => (
            <button
              key={c}
              type="button"
              onClick={() => setCurrency(c)}
              className={`px-4 py-2 rounded-lg text-xs font-bold transition-all ${
                currency === c
                  ? 'bg-white text-blue-600 shadow-xs'
                  : 'text-slate-600 hover:text-slate-900'
              }`}
            >
              {c === 'USD' ? 'USD ($)' : c === 'SGD' ? 'SGD (S$)' : 'IDR (Rp)'}
            </button>
          ))}
        </div>
      </section>

      {/* Pricing Cards Grid */}
      <section className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8 items-stretch">
          {PACKAGES_EN.map((pkg) => (
            <div
              key={pkg.id}
              className={`relative bg-white rounded-3xl border flex flex-col justify-between p-8 transition-all ${
                pkg.isPopular
                  ? 'border-2 border-blue-600 shadow-lg shadow-blue-900/5 ring-4 ring-blue-50'
                  : 'border-slate-200 shadow-sm hover:shadow-md'
              }`}
            >
              {pkg.isPopular && (
                <div className="absolute -top-3.5 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full text-[11px] font-extrabold bg-blue-600 text-white uppercase tracking-wider shadow-xs">
                  Most Popular for B2B
                </div>
              )}

              <div>
                <h3 className="text-xl font-bold text-slate-900">{pkg.name}</h3>
                <p className="text-xs text-slate-500 mt-2 leading-relaxed">{pkg.tagline}</p>

                {/* Price Display */}
                <div className="mt-6 p-4 rounded-2xl bg-slate-50 border border-slate-100">
                  <div className="text-xs text-slate-400 font-medium uppercase tracking-wider">
                    Investment ({currency})
                  </div>
                  <div className="text-3xl font-extrabold text-slate-900 mt-1">
                    {pkg.prices[currency]}
                  </div>
                  <div className="text-[11px] text-slate-500 mt-1">
                    One-time development &bull; Zero monthly software royalty
                  </div>
                </div>

                {/* Specs Pill List */}
                <div className="mt-6 space-y-2 py-4 border-y border-slate-100 text-xs text-slate-700">
                  <div className="flex items-center gap-2">
                    <Clock className="w-4 h-4 text-blue-600 shrink-0" />
                    <span><strong>Turnaround:</strong> {pkg.turnaroundDays}</span>
                  </div>
                  <div className="flex items-center gap-2">
                    <Sparkles className="w-4 h-4 text-amber-500 shrink-0" />
                    <span><strong>Page Capacity:</strong> {pkg.maxPages}</span>
                  </div>
                  <div className="flex items-center gap-2">
                    <ShieldCheck className="w-4 h-4 text-emerald-600 shrink-0" />
                    <span><strong>Revisions:</strong> {pkg.revisionCount}</span>
                  </div>
                </div>

                {/* Feature Checklist */}
                <div className="mt-6">
                  <span className="text-xs font-bold text-slate-900 uppercase tracking-wider block mb-3">
                    Deliverables Included:
                  </span>
                  <ul className="space-y-2.5 text-xs text-slate-600">
                    {pkg.features.map((feature, i) => (
                      <li key={i} className="flex items-start gap-2">
                        <CheckCircle2 className="w-4 h-4 text-emerald-600 shrink-0 mt-0.5" />
                        <span>{feature}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>

              {/* Action Buttons */}
              <div className="mt-8 pt-6 border-t border-slate-100 space-y-3">
                <a
                  href={getWhatsAppUrlForPackage(pkg.name)}
                  target="_blank"
                  rel="noopener noreferrer"
                  className={`w-full inline-flex items-center justify-center gap-2 py-3.5 px-4 rounded-xl text-xs font-bold transition-all ${
                    pkg.isPopular
                      ? 'bg-blue-600 hover:bg-blue-700 text-white shadow-md'
                      : 'bg-slate-900 hover:bg-slate-800 text-white'
                  }`}
                >
                  <MessageCircle className="w-4 h-4" />
                  <span>Inquire via WhatsApp</span>
                </a>
                <Link
                  href="/configure"
                  className="w-full inline-flex items-center justify-center gap-1.5 py-2.5 px-4 rounded-xl text-xs font-semibold text-slate-700 hover:bg-slate-100 transition-colors"
                >
                  <span>Customize with Configurator</span>
                  <ArrowRight className="w-3.5 h-3.5" />
                </Link>
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* FAQ Section */}
      <section className="py-16 bg-white border-t border-slate-200">
        <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="text-center mb-10">
            <span className="text-xs font-bold uppercase tracking-wider text-blue-600 bg-blue-50 px-3 py-1 rounded-full border border-blue-200">
              Clarity &amp; Assurance
            </span>
            <h2 className="text-2xl sm:text-3xl font-extrabold text-slate-900 mt-2">
              Package &amp; Contract FAQs
            </h2>
          </div>
          <FAQAccordion items={PACKAGES_FAQS_EN} />
        </div>
      </section>
    </div>
  );
}
